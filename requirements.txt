# CSR RAG Chatbot - Python Dependencies
# Install with: pip install -r requirements.txt

# PDF Processing (latest version supports Python 3.14)
PyMuPDF>=1.24.0

# LangChain and RAG (updated versions)
langchain>=0.1.0
langchain-community>=0.0.13

# Vector Database (use specific version that has pre-built wheels for Windows)
chromadb==0.4.15

# Embeddings (Sentence Transformers)
sentence-transformers==2.3.1

# LLM API
groq==0.4.1

# Utilities
python-dotenv==1.0.0

# Optional: For Jupyter notebooks
ipykernel==6.29.0
jupyter==1.0.0

# CSR RAG Chatbot - Streamlit Cloud Requirements
# Optimized for deployment

# Core dependencies
streamlit==1.40.1
python-dotenv==1.0.0

# LangChain
langchain==0.3.0
langchain-community==0.3.0
langchain-core==0.3.0

# Vector store
faiss-cpu==1.8.0

# Embeddings
sentence-transformers==3.3.1

# LLM API
groq==0.11.0

# PDF processing (not needed for deployment but keep for reference)
# PyMuPDF>=1.24.0